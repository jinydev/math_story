---
layout: "docs"
title: 'Math Story'
---

y=a'(a>1)

0                              x
at 0보다 크고 1보다 작은 경우
&
2=@(0<02<1)
0                              va

a7} LEU} 큰 경우의 그래프를 보면, *의 값이 커질수록>축 오
른쪽으로 갈수록 8의 값도 커져 전체적으로 보면 오른쪽 위로 올라가
는 모양이 됩니다. 그리고 지수적 증가의 특징으로 xo] 값이 일
정하게 커져도 의 값은 차이가 점점 크게 나며 커집니다. 이것
은 앞에서 단리법과 복리법을 통해 알 수 있었습니다.

@가 1보다 작은 경우 그래프를 보면, 이때에는 2의 값이 커질

”

수록 의 값은 작아지고 오른쪽 아래로 향하는 모양이 됩니다.

뉴턴이 들려주는 지수함수와 로그함수 이야기